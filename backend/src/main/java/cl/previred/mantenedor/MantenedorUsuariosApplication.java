package cl.previred.mantenedor;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * MantenedorUsuariosApplication es la clase principal de la aplicación Spring Boot.
 * Se encarga de iniciar el contexto de la aplicación y configurar los componentes necesarios.
 *
 * @author Roberto Salinas
 * @version 1.0
 */
@SpringBootApplication
public class MantenedorUsuariosApplication {
    public static void main(String[] args) {
        SpringApplication.run(MantenedorUsuariosApplication.class, args);
    }
}
